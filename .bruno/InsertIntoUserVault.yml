info:
  name: InsertIntoUserVault
  type: http
  seq: 7

http:
  method: POST
  url: "{{BASE_URL}}/user/vault"
  headers:
    - name: Content-Type
      value: application/octet-stream
  body:
    type: text
    data: My test vault data 12345!
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
