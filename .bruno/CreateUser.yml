info:
  name: CreateUser
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/user"
  body:
    type: json
    data: |-
      {
        "email": "test@test.it",
        "name": "test",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
