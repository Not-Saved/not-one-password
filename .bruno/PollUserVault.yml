info:
  name: PollUserVault
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/user/vault/poll"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
